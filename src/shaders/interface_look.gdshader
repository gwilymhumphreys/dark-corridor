shader_type canvas_item;
render_mode unshaded;
// Interface look (docs/systems/interface_look.md). The whole shader is in interface_look.gdshaderinc,
// shared with interface_portrait.gdshader.

#include "res://src/shaders/interface_look.gdshaderinc"
