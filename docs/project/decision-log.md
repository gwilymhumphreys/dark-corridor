# Dark Corridor — Decision Log & Handoff

> **For the next agent (or future-me).** Read [`handoff.md`](handoff.md) first (the fresh-agent orientation), then this. This records *what was decided, why, the approach, what's open, and what's next* — so you don't re-litigate settled calls or contradict the model. Dates: 2026-06-04; updated 2026-06-05 (block bypass is a per-effect `unblockable` flag; enemy-item targeting; prototype redefined as a playable itch.io build); build-prep 2026-06-05 (GDScript content definitions; `seq_id` component order; the scene-tree / node model — #23–25).

## Where the project is

Pre-prototype. The **only code** is the corridor renderer (`src/scenes/corridors/` + the `corridor_testbed` host) — that's "experimenting," and the real game will reuse the corridor as a *background*. **Everything else is paper** (design + PRDs in `docs/project/`). The **full prototype-scope spec is now written**: the foundation + combat spine (06-04), then the session/run-structure (`Game` / `Run` / `Encounter` / `Draft`), the content layer (relics/enchants/consumables), and presentation (`UI` / `VFX`) (06-05).

**Build status (updated 2026-06-05):** **Phase 1 — the combat spine — is built and green.** The build-prep decisions (#23–25), then Steps 1–5 of [`phase1_plan.md`](phase1_plan.md): `Timekeeper` · `Actor` · `StatusManager` · `Item` · `CombatManager` + a minimal **opaque** VFX wall + the `combat_sandbox` host. **45 GUT tests pass headless on 4.6; the feel gate passed.** Code now lives in `src/combat/`, `src/content/`, `src/autoloads/status_manager.gd`, `src/data/balance.gd` (placeholder tuning), `src/vfx/`, `src/scenes/combat/`, `tests/`. GUT is vendored in `addons/gut/`. **Phase 2 — autotest scaffolding — is built and green** (2026-06-05): the Mode/Driver/Logger trio + `AutoTestStuckDetector` in `src/autotest/` (dedicated `autotest.tscn`) drive ONE deterministic headless fight to a verdict via `CombatManager.sim_step()` — flags, forced nosave/notutorial, game-time `--timeout` + `--wall-timeout` + stuck detection, damage-by-family logging + a markdown report, exit `0` (resolved) / `1` (stuck/timeout). `--seed` / `--speed` are plumbed-but-inert until the run RNG (Phase 3). A Phase-1 review pass first hardened the spine (heal guard, Delivery pruning, seq_id doc). **Phase 3 — the run loop — is built and green** (2026-06-05, [`phase3_plan.md`](phase3_plan.md)): `Save` (autoload, JSON snapshot, atomic, no migration) + minimal `Relic` (combat-start status) + `Draft` (autoload, seeded 1-of-3) + `Encounter` (fight/rest, in `src/run/`) + `RunManager` (map, run-state, sequencing, HP-economy, snapshot/rehydrate, in `src/run/`) + `GameManagerAutoload` (`Game`: phase machine + run lifecycle). The autotest now drives a whole headless descent (`run_full`) — draft → fight → advance → win, deterministic by `--seed` (now live), with quit/resume. New autoloads `Save` / `Draft` / `Game`; new dir `src/run/`. **Fast-follow done (2026-06-06):** the **enchant** (Whetstone, scale-a-value on the board) + **consumable** (Healing Draught, thrown self-heal via `CombatManager.throw_consumable`) categories — so all three content categories prove their path end-to-end in the run + autotest. Also resolved **decision #26** (statuses are combat-scoped; persistence = relics/enchants) and fixed the **`Actor`↔`Item` RefCounted leak** (`Actor.dissolve()` at discard) + made status advancement uniform across actors and items. **112 GUT tests pass headless on 4.6.** **Phase 4 — real UI / the run screen — is built and green** (2026-06-06, [`phase4_plan.md`](phase4_plan.md)): the presentation tree (`main.tscn` → `MainController` → title / run / outcome screens) + the **real-time seam** (the run screen drives `CombatManager.tick` each physics frame; the logic tree stays *out* of the scene tree — the autotest path is byte-identical) + the **framed** combat view (corridor + thorn-demon **occupant** top-right via `CorridorScaled.axis_scale`, player left, board strips / HP / potions, the VFX wall) + slow-mo-on-hover + the run-screen **FSM** (approach → fight → draft → advance → win/death) + the **draft overlay** + the **map strip** + the **corridor approach** (the enemy scales from depth). New dirs `src/scenes/screens/` + `src/scenes/combat/` (the view); `main_scene` flipped to `main.tscn`. Full doc: [`../ui/run_screen.md`](../ui/run_screen.md). Deviations: the `RunManager.advancing` signal was unneeded (the run screen orchestrates the advance + approach directly, keeping `RunManager` presentation-free); one parameterized `outcome_screen.tscn` instead of two. **126 GUT tests pass headless on 4.6.** Two follow-ups done (128 tests): the **potion-throw UI** (clickable slots → `RunManager.throw_potion`) + the **localization POT pipeline** (`tools/extract_pot.gd` → `locale/`, registered in `project.godot`; [`localization.md`](../reference/localization.md)). **Phase 5 — the `tune` machinery — is built** (2026-06-06, [`phase5_plan.md`](phase5_plan.md), 139 tests): seeded board-aware **draft strategies** (`--strategy` live) + a **per-encounter / per-item report** (durations vs window, HP attrition, item fires + damage + trap-pick flags) — the two things the `tune` workflow needs. **Non-content backlog item — settings/pause + battle-speed — built** (2026-06-06, 153 tests): the **×1/×2/×3 battle-speed dial** (a `Game` session preference + an always-visible HUD toggle, applied to each fight's `Timekeeper` base scale) + in-run **pause** (a run-screen gate, **not** a `Game` phase) with a pause menu (Resume / Quit-to-menu, the save preserved so Title's Resume re-enters the beat); also **resolved the timescale replace-vs-multiply open → replace** (absolute slow-mo, independent of the dial). **Non-content backlog item — DoT per-applier attribution — done** (2026-06-06, 158 tests): the tune report's damage-by-family + item-contribution table now credit poison-tick damage to the **item that applied it** (a pre-step status snapshot reads `Status.source`), so Venom Fang shows its real poison damage instead of a generic "Poison" lump reading 0 — sharpening what `tune` reads (in `src/autotest/`). **Run structure + reward routing + status-build engine seams — built (2026-06-07):** the **multi-act run** (`RunMap` = 3 acts × 15 beats; act-end bosses, a fixed midpoint relic + a per-act rest, the rest **CHOICE** beats) with the **choice layer** (`has_pending_choice` / `pending_choice` / `pick_path`, drawing 2-3 seeded candidates), a **two-tier choice UI** (`choice_overlay`) + a placeholder **Event** type (`event_overlay` / `pick_event_option`, prose + binary outcome), the run-screen FSM gaining CHOOSING / EVENTING states, and the **HP economy** (between-act full heal, per-act rest) (#1, #16); **reward routing** — a relic on the **RELIC** reward + a relic-and-draft on the new **ELITE** reward, drawn from `RelicCatalog.REWARD_POOL` on the run RNG (deterministic / resume-stable), plus a relic `MAX_HP_BONUS` shape (#2); **per-fight RNG** (`_combat_seed_for` = run seed + encounter index) + a random-targeting item exercising the derived per-fight stream (#14, #20); the **spore-engine seams** Cap 1+2 ([`spore_engine_prd.md`](spore_engine_prd.md)) — `StatusManager.consume(target, type, amount)` stack-spending (self-fuel in the Item pipeline, opponent-fuel in the Combat-manager spawn path) + an **evasion** fizzle seam (`causes_evasion` → `Delivery.evaded`, a whiff with a reason, distinct from silence/`gate` = inert); and **mid-fight roster changes** Cap 3 — the `CombatManager` **two-roster** model (player side = run actor + run-scoped `allies` + combat-scoped `_player_tokens`; enemy side = `enemies`) with `add_actor` (combat-scoped summon), `register_ally` (run-scoped mid-fight ally), `Delivery.Kind.SUMMON`, and scope-aware teardown — so **allies support both run and combat scope** (#22). All placeholder content; the mechanisms are wired. **Content scaffolding built (2026-06-07, 217 tests):** content ids are now **string ids** (#23, amended from int enum), `src/content/` is reorganized into **kind-grouped subdirs**, a **minimal character system** (`CharacterDef`/`CharacterCatalog`) drives the start kit + a **per-character draft pool** (#27), and a shared **colorless pool** (empty by default) is appended at draw. **Multi-actor combat view + pause-anytime + mid-fight ally wiring — built (2026-06-08, 223 tests):** the framed `CombatView` now renders a **board strip per actor** (every enemy in the right column, the player's prominent board bottom-left, each run-scoped ally / combat-scoped summon token in a column beside it — reading the CM rosters each frame, so mid-fight summons appear as they spawn; `actor_pos` resolves each to its own strip) (#4); **pause is available at any point** in a live run (a run-screen gate, the menu's full-rect catcher blocking the board underneath) (#8); `RunManager.add_ally` registers a run-scoped ally into the **live** fight mid-combat (#6); plus a review pass — catalog `get_def` id-guards (an unknown id `push_error`s instead of a null-deref crash), a periodic-status-on-item guard, and `_payload_from_effect` / `throw_consumable` summon+consume field parity. **Next: the deferred content push** — scale the per-character item / enemy / encounter pools, the boss signature mechanics + the Mushroom Druid spore content, then a real `tune` pass.

## The approach (keep doing this)

- **One file per system.** `docs/project/<system>_prd.md`. Cross-system edges live **once** in `architecture.md` → *Interface contracts (boundary hub)*; PRDs link there, never duplicate edges.
- **Incremental.** Foundation + combat-adjacent are PRD'd. **Run-structure (`Run manager` / `Encounter` / `Draft`) is now prototype-scope** — the prototype target is a **playable itch.io build** (see Next steps), so it needs the run controller, encounters/events, fights, and localization. **Meta / characters stay deferred** until that build exists (per architecture's "don't write all PRDs up front"). The separate files are *so* we can add layers one at a time.
- **PRD template:** Purpose (is / is-not) → Boundaries (link to hub) → Model & rules → Prototype scope → Open/deferred → Dependencies. Numbers point to source files, never baked into prose. Open questions are **flagged, not invented**.
- **Think-first, then review.** Before writing a PRD, reason through ambiguities, surface *genuine* forks to the user (not conventional defaults), resolve, then write. After writing, consistency-review against the other docs.
- **House style:** sister projects `../battledraft`, `../dogmage` (shared systems, the `Manager` naming). `../a-machine` (= AMTKAG, the dev's shipping game) is the model for the autotest + `tune` patterns.

## The docs (current set)

`design.md` (whole-game snapshot) · `architecture.md` (system map + boundary hub + combat spine) · `combat_prd.md` (resolution model) · `timekeeper_prd.md` (the combat clock) · `actor_prd.md` · `status_manager_prd.md` · `item_prd.md` · `combat_manager_prd.md` (per-fight orchestrator) · `game_manager_prd.md` (session singleton) · `run_manager_prd.md` (the descent) · `encounter_prd.md` (the beat) · `draft_prd.md` (the reward draw) · `enemy_prd.md` · `content_prd.md` (relics / enchants / consumables) · `spore_engine_prd.md` (deferred engine seams the first status-identity character needs — consume / evasion / player-side summon) · `save_prd.md` · `ui_layout_prd.md` (the screen + input) · `vfx_driver_prd.md` (the combat wall) · `art_audio.md` (art/audio vibes) · `../testing/autotest.md` (harness scaffolding) · `.claude/commands/tune.md` + `.claude/agents/tune-run.md` (tune scaffolding).

## Key decisions + why

*(Naming evolved — don't resurrect old terms: `Core`/`Main` → `Timekeeper`; `…controller` → `…manager`; `StatusEngine` → `StatusManager`; `effect`/`pending effect` → `Delivery` + `payload`.)*

1. **`Timekeeper` (was Core/Main).** The combat time system is **combat-scoped** (resets each fight, no meaning outside) — "Core" wrongly implied global/persistent.
2. **`Manager` naming** (house style). Instanced → `class_name FooManager`; autoload → `class_name FooManagerAutoload` registered `Foo`. (`StatusEngine`→`StatusManager`; the two "controllers" → `Run manager` / `Combat manager`.)
3. **Testbed renamed** `main` → `corridor_testbed` (it's a renderer testbed, not the game; frees `main`).
4. **`Combat manager` introduced** — the per-fight orchestrator that nothing owned: the live fight (actor pair + left-to-right ordering), win/loss, runtime targeting authority, the trigger event bus, the component registry, **and the `Timekeeper`'s lifecycle**. The `Run manager` does **not** touch the Timekeeper (the user was explicit) — it hands a fight to the Combat manager and waits for the result.
5. **`StatusManager` = stateless-rules autoload.** Status *instances* live on their **targets** (Actor / Item), not in the manager — purely so the autoload holds no per-fight state. (Statuses themselves are combat-scoped, never run-persistent — **#26**; instances-on-targets is *not* a persistence mechanism.) **Block** = an actor-targeted **absorb-status**; `Actor.take_damage` runs incoming damage through the target's modifier-statuses via the StatusManager (block absorbs before HP; precise amplifier/absorber order is open — StatusManager PRD). Block **persists until consumed** (no decay) and absorbs all damage except `unblockable` payloads — a **per-effect** flag (varies by DoT), not a blanket DoT rule.
6. **Relics & potions = run-level** (player run-state `{ actor, relics, potions, … }`), **not** Actor-owned — keeps the Actor a pure, symmetric combatant (HP + board + statuses).
7. **Symmetric `Actor`; `Enemy` is not a class** — an Actor built from an authored *enemy definition*. Enemy items are a **content category** (per-enemy attack item + small shared utility pool), mechanically just `Item`s.
8. **`Delivery` + `payload`.** What an item fires = a **Delivery** (the in-flight carrier: `payload` + resolved target + travel-Ticker); its content = a **payload** `(kind, value)`. Retired "effect" as the combat-resolution word (overloaded). The item declares a **relative target-shape** (self / opponent-leftmost / all-opponents, + enemy-item shapes — see #14); the **Combat manager resolves shape→target and spawns the Delivery** — so the Item never depends *up* on the Combat manager.
9. **Fixed timestep (the big one).** Combat runs in fixed `STEP`s. The **`Timekeeper` is slimmed to the combat clock**: `sim_time` (stepped, for logic), `render_time()` (continuous, for the smooth VFX wall), the one speed `dial`, and `steps_due()` (real time × dial → whole steps, `MAX_STEPS` cap + backlog-drop) + `advance()`. The **`Combat manager` owns the component registry, advances every component each `sim_step()`, and runs the `_physics_process` loop**. The **dial = how many fixed steps run per real second** (not a per-step delta). This gives determinism + **bit-reproducible autotest** (run K steps); `render_time` keeps motion smooth between steps; Godot's physics loop handles the fixed-`delta` cadence + catch-up cap; **autotest drives `sim_step()` directly** (no real time).
10. **Item types:** every item is **active** (ticks); a "trigger" is an **additional event-push on the same accumulator** (the item still ticks — not a separate type); **no passive item type** (passive/always-on effects are **statuses**).
11. **`Save` = push, not pull.** The `Run manager` hands `Save` a snapshot on encounter entry; on launch the `Game manager` reads it back and the `Run manager` rehydrates — `Save` reads no live state. Auto-save on encounter entry, single run slot, **no migration** (`CLAUDE.md`). Snapshot = run-persistent state only (HP, board+enchants, relics, potions, position, **RNG** so resume is deterministic / not save-scummable); combat state is ephemeral, never saved.
12. **Triggers = accrual-only** (combat_prd's Bazaar lesson — loop-proof). The **event bus lives in the Combat manager**; a push fires on the *next* step → **one link per step** (no in-step recursion).
13. **Autotest scaffolding.** The AI driver is just **another input-intent source** (combat is auto, so it only makes draft / choice / potion decisions); `--speed` = the Timekeeper dial; headless skips the output layer; determinism = seeded RNG. **The decision AI and the `tune` skill internals are deferred.**
14. **Item-targeting (enemy items) = random by default.** Effects that hit an opponent's *items* (silence, item-debuffs) get two shapes — `opponent-item-random` (one) and `all-opponent-items` (all); single-item selection is **random via the seeded combat RNG** (fights stay bit-reproducible). A deliberate exception to the actor rule ("leftmost, never random," for player predictability) — provisional, may become a rule after testing.
15. **`Run manager` split into `Game manager` + `Run manager`.** The old "Run manager" had grown to own session, run, *and* save concerns. Split by lifetime: **`Game manager`** (autoload `Game`, session-lifetime) owns the **game-state machine** (title → run → death → meta), the **run lifecycle** (create / resume / end the run), and the **save-lifecycle** calls (`read` on launch, `clear` on death/win, meta-save) + global "call-from-anywhere" hooks. **`Run manager`** (instanced per run) owns the **map + encounter sequencing + player run-state + HP-economy**, and **writes** the per-encounter snapshot. Completes the per-lifetime orchestrator pattern — **Game (session) → Run (descent) → Combat (fight)**. *`Game` is an autoload by deliberate exception:* a session-lifetime singleton holding session state matches the autoload lifetime (nothing to keep "fresh"); the guardrail is that **per-run state stays in the instanced `Run manager`** (`Game` holds only a reference, null between runs).
16. **`Encounter` = the per-beat tier; the choice layer's *assembly* is the `Run manager`'s.** An `Encounter` is one resolved beat (fight / event / rest), instanced per beat by the `Run manager`; a fight `Encounter` creates the `Combat manager`. Tier: **Game → Run → Encounter → Combat**. The 2–3 candidate options at a choice point are **drawn by the `Run manager`** (a pool-draw under act constraints + RNG — it owns the map); UI presents; the picked candidate becomes the live `Encounter`. The Encounter is the resolved unit, not the selector; tier-2 within-choices (an event's binary, an elite's engage-by-picking) are the Encounter's own resolution. Rewards: the `Encounter` reports its outcome + reward-kind up; the `Run manager` fulfills it (drives `Draft` / grants the relic).
17. **`Draft` = a stateless reward-draw service.** `Draft` (autoload, like `StatusManager` / `Save`) answers "what candidates to offer" — slot composition (usually item; low chance enchant/potion), **depth-weighting** (rarity odds rise with depth), seeded from the **run RNG** (offer fixed per run-state — no save-scum). The **`Run manager` holds the pending offer and applies the pick** (board / potion / enchant-target / relic); `Draft` writes nothing. **Weighting is depth/rarity only — never the build/archetype** (the design's rejected hidden-weighting). **No skip** (no cap → taking one is always correct; the decision is which).
18. **The next encounter approaches from depth (created after the draft).** Rather than advance-then-spawn, the `Run manager` **creates the next `Encounter` right after the reward/draft** (spawning its enemy `Actor`s at the vanishing point) and **auto-saves** (encounter entry); the corridor advance then **animates the approach** — the enemies scale up from depth into full view, so the walk *is* the encounter arriving, not dead time. **Resolution begins on arrival** (a fight `Encounter` creates its `Combat manager` then). The approach is presentation (corridor renderer + UI); the logical beat is the resolution.
19. **Content layer (Relic / Enchantment / Consumable) → one combined PRD, prototype-scope.** The three were referenced across the loop but unspecced; they're **thin** (lean on `StatusManager` / `Combat manager` / `Item`), so one `content_prd.md` covers all three rather than three files. A relic = a persistent run-state modifier (combat-start status / triggered Ticker / direct mod); an enchant = a one-per-item modifier hooking the host item's fire/resolve; a consumable = a manually-fired reserve (a Delivery on throw, no Ticker). Split the file later if one grows. All three are **`Draftable`s** alongside `Item` (drafted / inspected / tooltipped); Enchantment differs only in *application* — it attaches to a chosen item, not its own slot.
20. **The `Run manager` owns the run RNG; its full state persists through save/load.** A single seeded PRNG drives all run-level randomness (draft offers, encounter assembly). The snapshot saves the **full RNG state** (not just the seed), so a reloaded save reproduces the **same future outcomes every time** — deterministic resume, no save-scumming a bad draft by quit-reload. Per-fight combat randomness (random item-targeting, #14) draws from a **derived per-fight stream** (run seed + encounter index), so combat doesn't perturb the run stream and a re-entered fight replays identically.
21. **`Draftable` = a composition contract, not a parent class.** What a draft can offer (item / relic / consumable / enchant) shares a **definition-face** (`id / name / icon / rarity / category / tooltip`) by composition; `Draft` + inspection are **category-blind**, and **application dispatches on `category`** (the one switch, in the `Run manager`). The **runtime entities stay distinct types** (they share almost nothing at runtime — a parent class would be a near-empty fat base). Consistent with the Ticker (owned, not subclassed) and Status (data + type key). `category` is **open** — adding a kind is additive.
22. **Entity model is already ally-ready — keep it that way (deferred experiment).** Two families — **`Actor`** (player + enemy, unified by symmetry) and **`Draftable`** (#21) — make "drafting allies" *additive*, not a refactor: an ally = a `Draftable` (`category: ally`) whose application spawns an `Actor` on the player's **side**. The `Combat manager` is already side-relative + list-based (N-vs-M in the abstraction; only 1-vs-M exercised today); the open `category` absorbs the new kind. **Don't build it now** (prototype stays 1-vs-M); **don't over-unify** (no über-`Entity` base over `Actor` + `Draftable` — they share nothing at runtime). The only single-player assumption is run-state's singular `actor` + its snapshot — cheap to change later (no save migration). **Forward-compat rule:** treat each side as a *party* (a roster of 1 today); don't hardwire "exactly one player `Actor`." Same "side = roster" need as the deferred summoning item.

23. **Content definitions are authored in GDScript, not data files.** Item / enemy / status / encounter / relic / enchant / consumable definitions are typed `RefCounted` def objects (`ItemDef`, `EnemyDef`, …) carrying the `Draftable` header by composition, constructed in a **static per-kind catalog keyed by a string id** (amended 2026-06-07 from an int `enum Id` to string ids — `ItemCatalog.WEAPON == 'weapon'` — so content files can be added/removed without editing a central enum). No JSON / `.tres` / inspector — author everything in code (the Slay-the-Spire-2 approach; chosen over last project's JSON, to try). Localization is preserved: source English on the def, displayed via `tr(def.name)`, with the catalog files added to the POT extraction list. Resolves the item / status / enemy / encounter / content data-format opens. *(The `Save` run-snapshot serialization format is separate and still open — it stores ids + runtime values, not defs.)* *(Organization, 2026-06-07: each kind's defs + catalog live in a grouped subdir — `src/content/{items,enemies,relics,consumables,enchants,encounters,statuses,characters}/`.)*
24. **Deterministic component order = a monotonic `seq_id`.** The `Combat manager` stamps each registered component (item Ticker / status / Delivery) an incrementing `seq_id` at registration and advances its registry in ascending `seq_id`. Registration order is itself deterministic (board order at fight start, then the deterministic fire order each step), so the sweep is bit-reproducible. Simplest thing that works — resolves the Combat-manager *within-step component order* open. *(Phase 1 realization, 2026-06-05: the sweep is built as fixed type-ordered passes — item cooldowns → statuses → Delivery travel, each in insertion order — which delivers the same deterministic, bit-reproducible order. A literal `seq_id` counter field is deferred until components of different types must interleave by registration time, which Phase 1 never requires.)*
25. **Scene tree = a logic tree (under the `Game` autoload) + a separate presentation tree (the main scene).** Combat logic is plain `RefCounted` (`Actor` / `Item` / `Status` / `Delivery` / `Timekeeper` / event bus); only the three orchestrators (`Run manager` / `Encounter` / `Combat manager`) are `Node`s, and **only the `Combat manager` runs `_physics_process`** (the one tick). The presentation tree (`main.tscn` → `ScreenHolder` → title/run/death screens; the run screen composing the corridor panel + a swappable `CombatView` + overlays) only *reads* the logic and emits intents — so a headless autotest just doesn't mount it. The **framed-vs-full-screen** open is isolated to the single swappable `CombatView` sub-scene. Full structure + directory layout: [`architecture.md` → Scene tree & node model](architecture.md#scene-tree--node-model). New dirs `src/combat/` · `src/run/` · `src/content/` · `src/vfx/` · `src/scenes/screens/` · `src/scenes/combat/`; `Balance` consts in `src/data/balance.gd`; GUT in `addons/gut/` + `tests/`.

26. **Statuses are combat-scoped; run persistence is Relics + Enchantments (status-lifetime resolved, 2026-06-05).** Every status (poison, block, burn, freeze, silence, this-fight item buffs/debuffs) is **combat-scoped** — created during a fight, cleared at `CombatManager.teardown()`, **never saved**. Statuses are *dynamic* combat states (accrue / decay / consume / expire) with no coherent cross-fight meaning (you don't carry a half-consumed block pool or a ticking poison between fights) — the Slay-the-Spire model the design follows (powers reset each combat; relics persist). **Durable state lives one layer up:** a **Relic** (run-level, saved — and a relic may carry its own counter) or an **Enchantment** (permanent item modifier, saved on the board) holds the magnitude, and re-expresses it as a fresh combat-scoped status each fight where needed — **Stone Ward** already does exactly this (a saved relic that applies a combat-scoped block status at each fight start). Consequence: **the run snapshot never serializes status instances** — only ids + values (board+enchants, relics, potions, HP, position, RNG). This **corrects the earlier framing** in #5 + the StatusManager PRD that statuses-on-`Item`s were a way to let item-buffs *outlive* a fight — they don't; that's the Enchantment's job. Instances live on their targets only to keep `StatusManager` **stateless**. Resolves the Save PRD's open "which statuses are run-persistent vs combat-scoped" → **none are**.

27. **Per-character item pools (departs from the single-shared-pool plan, 2026-06-07).** Each character draws from its **own** item pool, not one shared pool across all characters (which `design.md` originally specced). **Why:** a 1-of-3 draft with **no hidden weighting** (rejected — [design](design.md) / [draft_prd](draft_prd.md)) can't be both *wide* and *connective* from one pool — few directions → drafts connect but the game is narrow; many directions → range but drafts stop connecting. Per-character pools dissolve the tradeoff by putting **focus per-run** (one character's few coherent directions) and **range per-game** (N× across the roster); pools can be **smaller and deeper** per character (unique synergies that wouldn't fit alongside everyone else's). **Hybrid rejected** (a shared neutral / Slay-the-Spire "colorless" commons layer + per-character anchors): our commons aren't thematically null like Slay the Spire's — a character's spore appliers are *also* its damage / block, so the "answer combat questions before synergy" need is met inside its own pool; a neutral layer would fight identity for no scope saving worth the cost. *(Refinement 2026-06-07: it's the colorless **layer** — a default tier characters depend on — that's rejected; individual **colorless items** are **not** banned and may still exist where they earn it (character-agnostic utility, or an item that fits several pools), just never as a workhorse-commons crutch.)* **Scope — split only the layer that needs depth:** **item pools** split per character; **enemies stay a shared pool**, the **reward-relic pool stays shared**, and per-character *everything* (relics / enemies / bespoke commons) is the **Battledraft scope trap** to avoid. *(The unique build-defining **starting relic** per character is unchanged — that's one bespoke relic, not a split pool.)* **Build impact — none to systems:** `Draft` already pulls from "the pool" and is category-blind (#21 — the `Draftable` contract); scoping the pool to the active character is a **content-organization** change. The only wiring: the `Run manager` hands the *character's* pool to `Draft.draw` instead of one global pool. **Watch:** directions-per-character is a starting peg, not a number (too few forces drafts; too many re-dilutes inside one character) — resolve in prototype. Full rationale: [`../design/per_character_pools.md`](../design/per_character_pools.md). **BUILT 2026-06-07:** a minimal `CharacterDef` + `CharacterCatalog` (a placeholder default character ports the prototype seed; the full Characters system stays deferred); `RunManager._draft_pool()` hands `character.item_pool + ColorlessPool.ITEMS` to `Draft` (the shared colorless pool is empty by default — the exception that earns it). 217 GUT tests green.

## Open / deferred (each has a home)

- Timescale override **replace-vs-multiply — resolved → replace** (absolute slow-mo, independent of the ×1/×2/×3 battle-speed dial; the dial is a `Game` session preference applied to each fight's Timekeeper base scale). timekeeper_prd / combat_prd.
- **Within-step component order — resolved (#24):** deterministic registration order; built in Phase 1 as fixed type-ordered passes (item cooldowns → statuses → Deliveries), with a literal `seq_id` counter deferred until cross-type interleaving is needed.
- **Stat-statuses** (strength/weak/vulnerable) → the two damage-modifier seams are now **BUILT**: `StatusDef.outgoing_damage_mult` (applied at fire time in `Item._resolve_effect`) + the incoming **amplifier** stage in `resolve_incoming_damage` (before block), both **% multipliers** (cascade-safe — a flat *per-fire* modifier makes fast items strictly dominant). Placeholder `Weak` / `Vulnerable` statuses + a `Sundering Bolt` applier prove the path; the real stat-status **content** (numbers, per-stack variants, which damage types amplify) stays the owner's, authored as GD `StatusDef`s ([design](design.md)).
- **Per-effect stack/decrement** + block tuning → content.
- **Mid-fight roster changes (summoning) — BUILT (Cap 3, 2026-06-08).** The Combat manager is now a **two-roster** model (no longer a fixed pair): `add_actor` spawns a combat-scoped summon on either side, `Delivery.Kind.SUMMON` lands it, scope-aware teardown dissolves enemies + player tokens but keeps the run-scoped player + allies. Exercises boss "summons-adds" + the player-side token path.
- **Drafting allies / multi-Actor player side** (#22) → the **multi-Actor player side is BUILT** (run-scoped `allies` + `register_ally` for a mid-fight add; the framed view renders a strip per ally/token, #4). **Still open:** the **acquisition path** — a `Draftable` `category: ally` (or an event/reward that grants one), so a run can actually *gain* an ally; the side stays a *party* (roster of 1 by default) and nothing hardwires one actor.
- **Status-build engine seams** (the Mushroom Druid — `docs/design/`) → **Caps 1+2+3 BUILT (2026-06-07/08)**, [`spore_engine_prd.md`](spore_engine_prd.md): (1) `StatusManager.consume(target, type, amount)` stack-spending (self-fuel in the Item pipeline, opponent-fuel in the Combat-manager spawn path); (2) an **evasion** fizzle seam (`causes_evasion` → `Delivery.evaded`, a whiff with a reason, distinct from silence/`gate` = inert); (3) the **player-side** mid-fight roster add (summon tokens / allies — the Cap 3 work above). **Still the content's:** the spore **appliers** (need nothing new — just authoring) + the lethal-spore execute / spawn-on-kill rider.
- **Simultaneous-death tiebreak** (→ loss, provisional); **AOE-at-arrival** specifics → when multi-enemy fights are built.
- **Game-state-machine ownership — resolved (#15):** the `Game manager` (autoload) owns it, not the `Run manager`.
- **`STEP` / `MAX_STEPS` values** → tuning. **Content definition format — resolved (#23):** GDScript def objects + catalogs. **Save serialization format** (the run snapshot) → impl, still open.
- **Localization — in scope from the prototype** (the playable itch.io build ships translatable). Item / status / enemy names + tooltips + encounter prose must be `tr()`-able / POT-extractable (`CLAUDE.md`); bake the constraint into each data-format decision from the start, never retrofitted.
- **The decision AI**, the **`tune` internals**, and the **autotest code** (`src/autotest/*.gd`) → build when the prototype combat loop exists.

## Next steps

1. **Prototype-scope PRDs — all written:** `Game manager` + `Run manager` + `Encounter` + `Draft` + `Content` (relics/enchants/consumables) + `UI/Layout` + `VFX driver` (#15–20), on top of the combat spine. **No PRD:** audio/music — `art_audio.md` covers it (dungeon synth + per-effect SFX, not a system to spec). **Still deferred (post-prototype):** `Meta-progression`, `Characters`. **Next is building, not speccing** — the prototype loop (see build order below).
2. **Build order for the prototype loop** (spine first, then layer the run loop on top — ship the loop, not a combat test-harness):
   1. **Foundation + combat spine** — `Timekeeper` + `Actor` + `StatusManager` + `Item` + `Combat manager`: one player vs. one authored enemy, fixed-step `sim_step` (advance → fire → land → events → win/loss). Add the **minimal VFX wall** + a **stub UI** so the fight is watchable. **→ detailed step-by-step in [`phase1_plan.md`](phase1_plan.md).**
   2. **Autotest scaffolding** — `AutoTestMode` / stub `AutoTestDriver` / `AutoTestLogger`: drive the fight headless + deterministic via the input-intents.
   3. **The run loop** — `Save` + `Game manager` (thin state machine: title → run → death → restart) + `Run manager` (map + sequencing + run-state + the run RNG) + `Encounter` (the beat + approach-from-depth) + `Draft` (the reward draw) + minimal `Content` (one relic / enchant / potion): draft → fight → advance through a short run, with resume.
   4. **Real `UI/Layout`** (boards / portrait / potions / the choice layer + the 1D map). **Localization** is threaded throughout — every player-facing string `tr()`/POT-extractable from the first one, not retrofitted.
   5. **Scale content + `tune`** — the item / enemy / encounter pools.
   *(Prototype = a **playable itch.io build**, resolved 2026-06-05: the loop above, not just one fight.)*
3. The design's own next step: "build the prototype loop (one corridor segment, 3 placeholder items, one enemy, draft, repeat ~10×); watch whether the cascade is satisfying."

## Conventions (from `CLAUDE.md`)

Static typing always; single quotes; 2-space indent; `snake_case` filenames; `PascalCase` `class_name`; autoloads `<Name>Autoload` registered `<Name>` (e.g. `SaveAutoload`, `DraftAutoload`, `StatusManagerAutoload`). Full names, not abbreviations. Docs describe **systems/intent, not numbers** (point to source). **No save migration.** Player-facing text is localizable (dev panels stay English). Run Godot 4.6; headless reimport after adding assets. The **boundary hub** in `architecture.md` is the canonical cross-system reference.
